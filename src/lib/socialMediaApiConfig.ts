/**
 * Social media monitoring API — server-only.
 * Proxied via `/api/bff/social-media/*`; uses the CRM backend (`API_BASE_URL`) with Bearer auth.
 */
export { API_BASE_URL as SOCIAL_MEDIA_API_BASE_URL } from "@/lib/apiConfig";
